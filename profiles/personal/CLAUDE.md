@~/.claude/shared-rules.md

# Personal Context

- Personal projects are on GitHub. No Jira tickets, no GitLab pipelines — use the
  scopeless or area-slug commit scope, and never block a change request on a ticket.
- `gh` is the forge CLI here; see `~/.claude/references/forge-cli.md`.
